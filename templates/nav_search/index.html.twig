{{ form_start(formSearchBar, {'action': path('boutique', {'pageCourante' : 1}) }) }}
    {{ form_widget(formSearchBar.rechercheNav) }}
    {{ form_widget(formSearchBar.save) }}
    {{ form_end(formSearchBar) }}

