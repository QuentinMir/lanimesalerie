{% extends 'base-admin.html.twig' %}

{% block title %}Edit Produit{% endblock %}

{% block body %}
    <h1>Edit Produit</h1>


    {{ include('produit/_form.html.twig', {'button_label': 'Update'}) }}
    <div class="d-flex">
        <div><a class="btn btn-outline-dark" href="{{ path('produit_index') }}">Retour à la liste</a></div>

        <div>{{ include('produit/_delete_form.html.twig') }}</div>

    </div>
{% endblock %}
