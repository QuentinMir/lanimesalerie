{% extends 'base-admin.html.twig' %}

{% block title %}Modifier Marque{% endblock %}

{% block body %}
    <h1>Modifier Marque</h1>

    {{ include('marque/_form.html.twig', {'button_label': 'Update'}) }}
    <div class="d-flex">
        <div><a class="btn btn-outline-dark" href="{{ path('marque_index') }}">Retour à la liste</a></div>

        <div>{{ include('marque/_delete_form.html.twig') }}</div>

    </div>

{% endblock %}
