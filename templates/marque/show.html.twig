{% extends 'base-admin.html.twig' %}

{% block title %}Marque{% endblock %}

{% block body %}
    <h1>Marque</h1>

    <table class="table mb-3">
        <tbody>
        <tr>
            <th>IdMarque</th>
            <td>{{ marque.idMarque }}</td>
        </tr>
        <tr>
            <th>Nom</th>
            <td>{{ marque.nom }}</td>
        </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-dark" href="{{ path('marque_index') }}">Retour à la liste</a>

    <a class="btn btn-outline-warning" href="{{ path('marque_edit', {'idMarque': marque.idMarque}) }}">Modifier</a>

    {{ include('marque/_delete_form.html.twig') }}
{% endblock %}
