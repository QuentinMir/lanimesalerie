{% extends 'base-admin.html.twig' %}

{% block title %}Marque{% endblock %}

{% block body %}
    <h1>Marque</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>IdMarque</th>
            <td>{{ marque.idMarque }}</td>
        </tr>
        <tr>
            <th>Nom</th>
            <td>{{ marque.nom }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('marque_index') }}">back to list</a>

    <a href="{{ path('marque_edit', {'idMarque': marque.idMarque}) }}">edit</a>

    {{ include('marque/_delete_form.html.twig') }}
{% endblock %}
