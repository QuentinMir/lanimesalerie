{% extends 'base.html.twig' %}

{% block title %}Carousel{% endblock %}

{% block body %}
    <h1>Carousel</h1>

    <table class="table mb-md-3">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ carousel.id }}</td>
        </tr>
        <tr>
            <th>Image</th>
            <td>{{ carousel.image }}</td>
        </tr>
        <tr>
            <th>Text</th>
            <td>{{ carousel.text }}</td>
        </tr>
        <tr>
            <th>Bouton</th>
            <td>{{ carousel.bouton }}</td>
        </tr>
        <tr>
            <th>Navigation</th>
            <td>{{ carousel.navigation }}</td>
        </tr>
        <tr>
            <th>Lien</th>
            <td>{{ carousel.lien }}</td>
        </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-dark" href="{{ path('carousel_index') }}">Retour à la liste</a>

    <a class="btn btn-outline-warning" href="{{ path('carousel_edit', {'id': carousel.id}) }}">Modifier</a>

    {{ include('carousel/_delete_form.html.twig') }}
{% endblock %}
