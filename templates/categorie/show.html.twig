{% extends 'base-admin.html.twig' %}

{% block title %}Catégorie{% endblock %}

{% block body %}
    <h1>Catégorie</h1>

    <table class="table mb-3">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ categorie.id }}</td>
        </tr>
        <tr>
            <th>Nom</th>
            <td>{{ categorie.nom }}</td>
        </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-dark" href="{{ path('categorie_index') }}">Retour à la liste</a>

    <a class="btn btn-outline-warning" href="{{ path('categorie_edit', {'id': categorie.id}) }}">Modifier</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
